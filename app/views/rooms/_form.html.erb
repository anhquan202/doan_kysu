<%= form_with(model: room) do |form| %>
  <% if room.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
        <% room.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :room_type, style: "display: block" %>
    <%= form.number_field :room_type %>
  </div>

  <div>
    <%= form.label :room_code, style: "display: block" %>
    <%= form.text_field :room_code %>
  </div>

  <div>
    <%= form.label :max_customer, style: "display: block" %>
    <%= form.number_field :max_customer %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
